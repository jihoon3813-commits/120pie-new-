"const fs = require('fs');\nconst iconv = require('iconv-lite');\n\ntry {\n  const content = fs.readFileSync('d:\\\\anti-gv\\\\25. 120pie(new)_2\\\\scratch\\\\diff_workdir_utf8.txt', 'utf8');\n  console.log(\"diff_workdir_utf8 length:\", content.length);\n  \n  // Try to decode CP949\n  const rawBuf = fs.readFileSync('d:\\\\anti-gv\\\\25. 120pie(new)_2\\\\scratch\\\\diff_workdir.txt');\n  const decoded = iconv.decode(rawBuf, 'cp949');\n  fs.writeFileSync('d:\\\\anti-gv\\\\25. 120pie(new)_2\\\\scratch\\\\diff_workdir_decoded.txt', decoded, 'utf8');\n  console.log(\"Saved decoded diff_workdir_decoded.txt (length:\", decoded.length, \")\");\n  \n  // Search for key words in decoded diff\n  console.log(\"Includes '120겹파이 올인원 패키지'?\", decoded.includes(\"120겹파이 올인원 패키지\"));\n  console.log(\"Includes 'What is Hybrid?'?\", decoded.includes(\"What is Hybrid?\"));\n  console.log(\"Includes '신규 가맹 정식 창업'?\", decoded.includes(\"신규 가맹 정식 창업\"));\n} catch (e) {\n  console.error(e);\n}\n"