"const fs = require('fs');\nconst content = fs.readFileSync('d:\\\\anti-gv\\\\25. 120pie(new)_2\\\\scratch\\\\franchise_staged_git.tsx', 'utf8');\nconst lines = content.split('\\n');\n\nconst keywords = [\"6,518\", \"올인원\", \"5-STEP\", \"6518\", \"hybrid\", \"창업 모델\"];\n\nconsole.log(\"Checking franchise_staged_git.tsx (lines:\", lines.length, \")\");\nfor (let i = 0; i < lines.length; i++) {\n  const line = lines[i];\n  for (const kw of keywords) {\n    if (line.includes(kw)) {\n      console.log(`Line ${i+1} matches \"${kw}\": ${line.trim()}`);\n      break;\n    }\n  }\n}\n"